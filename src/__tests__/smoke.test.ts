/**
 * Smoke test — verifies the test harness itself is wired up correctly.
 * Real component/unit tests live alongside their source files.
 */
describe("smoke", () => {
  it("truth is truthy", () => {
    expect(true).toBe(true);
  });
});
