/**
 * auth/ — Public API for auth guard components
 *
 * Usage:
 *   import { RequireAuth, RequireAdmin, AuthCallback } from '@/components/auth'
 */

export { RequireAuth, RequireAdmin } from './RequireAuth'
export { AuthCallback } from './AuthCallback'
